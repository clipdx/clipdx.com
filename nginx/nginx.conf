server { 
  listen 80;
  server_name .clipdx.com;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  server_name .clipdx.com;
  # Location bind-mounted in docker-compose.yml
  ssl_certificate /etc/letsencrypt/live/clipdx.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/clipdx.com/privkey.pem;
  location / {
    root /usr/share/nginx/html;
    # Untested: $uri.html $uri/index.html idea
    # to capture e.g. /webday -> webday.html or /webday -> /webday/index.html ?
    try_files $uri $uri.html $uri/index.html /index.html;
    index /index.html;
  }
}
